LONGEVITY CO. — BULK UPLOAD / EDIT + HOME UNIFORM — 43 BUILD

REPLACE:
- admin-sort.js
- index.html

ADD:
- admin-bulk-plus.js
- admin-bulk-plus.css

ADMIN CHANGES
- Adds "Bulk Upload / Edit" to the expanded main admin navigation.
- Keeps the existing image filename grouping workflow.
- Adds a clear "Add Blank Product" workflow so products can be added manually to the batch.
- Adds "Choose Product Photos" as a direct batch action.
- Every product card gets its own "Add Images to This Product" action.
- Images added to a specific card use the existing cover / hover / gallery ordering tools.
- Manual items use the same product fields, sizes, inventory, collections, duplicate/remove actions and final Shopify upload flow as image-created items.
- Adds a simple four-step workflow guide.
- Mobile layout is included.
- No API changes and no new Shopify permissions/environment variables.

HOME CHANGES
- Adds Uniform directly after Shop in the special homepage navigation.

IMPORTANT
- Existing admin-bulk.js is NOT replaced.
- Existing Shopify upload/save logic remains the source of truth.
- Existing Admin Ops, Shop Editor, mobile admin, sorting and bulk-edit stability add-ons remain loaded.
